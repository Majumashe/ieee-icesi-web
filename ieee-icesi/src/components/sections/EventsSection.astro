---
/**
 * EVENTS SECTION — IEEE ICESI (inicio)
 * Muestra un resumen de eventos en la home: próximos arriba, pasados abajo.
 * Usa el componente reutilizable EventCard (mismas tarjetas que /events).
 * Datos de events.ts (clasificación automática por fecha).
 * By: Majumashe - WebMaster 2026
 */

import Button from "../ui/Button.astro";
import EventCard from "../cards/EventCard.astro";
import { getUpcomingEvents, getPastEvents } from "../../data/events";

const upcoming = getUpcomingEvents();
const past = getPastEvents().slice(0, 3); // solo 3 pasados en el inicio
---

<section class="events-themed py-20">
  <div class="mx-auto max-w-screen-xl px-6 lg:px-12">
    <!-- Encabezado -->
    <div class="mb-12 flex flex-wrap items-end justify-between gap-4">
      <div>
        <div class="mb-4 border-l-[3px] border-ieee-blue pl-3">
          <span
            class="text-xs font-medium tracking-[1.5px] text-ieee-blue uppercase"
          >
            Eventos
          </span>
        </div>
        <h2 class="events-title text-3xl font-bold tracking-tight lg:text-4xl">
          Lo que está pasando
        </h2>
      </div>
      <Button href="/events" variant="outline">Ver todos los eventos</Button>
    </div>

    <!-- PRÓXIMOS -->
    {
      upcoming.length > 0 && (
        <div class="mb-16">
          <h3 class="events-subhead mb-6 text-sm font-bold tracking-[0.15em] uppercase">
            Próximos eventos
          </h3>
          <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
            {upcoming.map((event) => (
              <EventCard event={event} />
            ))}
          </div>
        </div>
      )
    }

    <!-- PASADOS -->
    {
      past.length > 0 && (
        <div>
          <h3 class="events-subhead mb-6 text-sm font-bold tracking-[0.15em] uppercase">
            Eventos pasados
          </h3>
          <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
            {past.map((event) => (
              <EventCard event={event} isPast />
            ))}
          </div>
        </div>
      )
    }
  </div>
</section>

<style>
  .events-themed {
    background-color: var(--hero-bg);
    transition: background-color 0.3s ease;
  }
  .events-title {
    color: var(--hero-text);
  }
  .events-subhead {
    color: var(--hero-text-muted);
  }
</style>